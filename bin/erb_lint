#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

arguments = [ "--config", File.expand_path("../.erb_lint.yml", __dir__) ]
arguments << "--lint-all" if ARGV.empty?
ARGV.unshift(*arguments)

load Gem.bin_path("erb_lint", "erb_lint")
