<div data-controller="dialog interaction-date" data-dialog-open-value="<%= open? %>">
  <%= render ButtonComponent.new(
        data: { action: "dialog#open interaction-date#setCurrentDate" },
        aria: { controls: DOM_ID, haspopup: "dialog" }
      ) do %>
    <%= t("interactions.contacted_today.button") %>
  <% end %>

  <dialog id="<%= DOM_ID %>"
          data-dialog-target="dialog"
          data-action="click->dialog#closeOnBackdrop"
          aria-labelledby="quick-interaction-heading"
          class="m-auto w-full max-w-sm rounded-xl p-0 shadow-xl backdrop:bg-stone-900/50 dark:bg-stone-800">
    <div class="p-6">
      <h2 id="quick-interaction-heading" class="text-lg font-semibold dark:text-stone-100"><%= t("interactions.follow_up.heading", name: friend.name) %></h2>
      <p class="mt-1 text-sm text-stone-500 dark:text-stone-400"><%= t("interactions.follow_up.hint") %></p>

      <%= form_with model: [ friend, interaction ],
            data: { interaction_date_target: "form", action: "submit->interaction-date#submit" },
            class: "mt-4 space-y-4" do |form| %>
        <%= hidden_field_tag :context, "quick_log" %>
        <%= hidden_field_tag :date_source, "server", data: { interaction_date_target: "source" } %>
        <%= hidden_field_tag :browser_date, nil, data: { interaction_date_target: "today" } %>
        <div>
          <%= form.label :occurred_on, t("interactions.follow_up.occurred_on"), class: "block text-sm font-medium text-stone-700 dark:text-stone-300" %>
          <%= form.date_field :occurred_on,
                required: true,
                max: Date.current,
                data: { interaction_date_target: "date" },
                class: "mt-1 w-full rounded-lg border border-stone-300 bg-stone-50 px-3 py-2 text-sm focus:border-amber-500 focus:outline-none focus:ring-1 focus:ring-amber-500 dark:border-stone-600 dark:bg-stone-800 dark:text-stone-100" %>
          <% interaction.errors.full_messages_for(:occurred_on).each do |message| %>
            <p class="mt-1 text-sm text-red-600"><%= message %></p>
          <% end %>
        </div>
        <div>
          <%= form.label :contact_method, t("interactions.follow_up.contact_method"), class: "block text-sm font-medium text-stone-700 dark:text-stone-300" %>
          <%= form.select :contact_method, contact_method_options,
                { include_blank: t("interactions.follow_up.method_blank") },
                class: "mt-1 w-full rounded-lg border border-stone-300 bg-stone-50 px-3 py-2 text-sm focus:border-amber-500 focus:outline-none focus:ring-1 focus:ring-amber-500 dark:border-stone-600 dark:bg-stone-800 dark:text-stone-100" %>
          <% interaction.errors.full_messages_for(:contact_method).each do |message| %>
            <p class="mt-1 text-sm text-red-600"><%= message %></p>
          <% end %>
        </div>
        <div>
          <%= form.label :note, t("interactions.follow_up.note"), class: "block text-sm font-medium text-stone-700 dark:text-stone-300" %>
          <%= form.text_area :note, rows: 3, placeholder: t("interactions.follow_up.note_placeholder"),
                class: "mt-1 w-full resize-none rounded-lg border border-stone-300 bg-stone-50 px-3 py-2 text-sm focus:border-amber-500 focus:outline-none focus:ring-1 focus:ring-amber-500 dark:border-stone-600 dark:bg-stone-800 dark:text-stone-100" %>
        </div>
        <div class="flex justify-end gap-2">
          <%= render ButtonComponent.new(variant: :ghost, data: { action: "dialog#close" }) do %>
            <%= t("interactions.follow_up.cancel") %>
          <% end %>
          <%= render ButtonComponent.new(type: :submit) do %>
            <%= t("interactions.follow_up.save") %>
          <% end %>
        </div>
      <% end %>
    </div>
  </dialog>
</div>
