<% content_for :title, @friend.name %>

<%= link_to t(".back"), root_path, class: "text-sm text-gray-500 hover:text-gray-900" %>

<h1 class="mt-4 text-2xl font-bold"><%= @friend.name %></h1>

<p class="mt-8 text-sm text-gray-500"><%= t(".no_entries") %></p>
