<% content_for :title, t("interactions.form.new_title", name: @person.name) %>
<div data-controller="interaction-date" data-interaction-date-time-zone-value="<%= Current.user.time_zone %>" class="mx-auto max-w-2xl">
  <h1 class="text-2xl font-bold"><%= t("interactions.form.new_heading", name: @person.name) %></h1>
  <div class="mt-6"><%= render InteractionFormComponent.new(person: @person, interaction: @interaction) %></div>
</div>
