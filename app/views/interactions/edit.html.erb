<% content_for :title, t("interactions.form.edit_title", name: @friend.name) %>
<div data-controller="interaction-date" class="mx-auto max-w-2xl">
  <h1 class="text-2xl font-bold"><%= t("interactions.form.edit_heading", name: @friend.name) %></h1>
  <div class="mt-6"><%= render InteractionFormComponent.new(friend: @friend, interaction: @interaction) %></div>
</div>
