<span class="text-sm">
  <% if entry.entry_date %>
    <% format = { "year" => :year, "month" => :month_year }.fetch(entry.date_precision, :long) %>
    <%= l(entry.entry_date, format: format) %>
  <% end %>
</span>
<% if entry.note.present? %>
  <p class="mt-1 text-sm text-stone-600 dark:text-stone-300"><%= entry.note %></p>
<% end %>
