<% date = entry.entry_date %>
<% if date %>
  <%= l(date, format: :long) %>
  <% if entry.age %>
    <span class="text-stone-500 dark:text-stone-400">(<%= entry.age %> <%= t("entries.age") %>)</span>
  <% end %>
<% end %>
